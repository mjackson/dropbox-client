{
  "name": "dropbox-client",
  "version": "1.0.0",
  "description": "Cross-environment JavaScript client for the Dropbox API",
  "author": "Michael Jackson",
  "license": "MIT",
  "main": "lib",
  "files": [
    "lib"
  ],
  "scripts": {
    "lint": "eslint modules",
    "test": "npm run lint",
    "build": "rimraf lib && babel ./modules -d lib",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "http-client": "^2.3.1",
    "query-string": "^3.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-eslint": "^5.0.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-1": "^6.5.0",
    "eslint": "^2.2.0",
    "eslint-config-airbnb": "^6.1.0",
    "eslint-plugin-react": "^4.2.3",
    "rimraf": "^2.5.2"
  }
}
